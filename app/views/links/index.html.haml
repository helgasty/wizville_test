%main.px-3
  %h3 Shorten your url
  %br/
  = form_for @link, url: { action: "create" } do |f|
    .form-group
      = f.url_field :base_url, class: 'form-control'
      %br/
      = f.submit 'Shortener', class: 'btn btn-primary'